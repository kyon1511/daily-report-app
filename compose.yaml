services:
  db:
    image: postgres:16
    shm_size: 128mb
    restart: always
    env_file:
      - ./Backend/.env
    ports:
      - "5434:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:

